import { MenuItem, Paper } from "@mui/material";
import styled from "styled-components";

export const LoginButtonContainer = styled(Paper)`
  @media (min-width: 768px) {
    position: absolute;
    width: 150px;
  }
`;

export const ItemMenu = styled(MenuItem)`
`
